#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

//Function Definitions
//vector<Car> createInventory();

struct Car {
    string name;
    string style; // Sedan/Coupe/SUV
    double rent_cost_per_day;
};

vector<Car> createInventory() {
    vector<Car> rV;
    return rV;
}

